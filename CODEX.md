## Pending Infrastructure Work

- [ ] Add CI job to run ./gradlew assembleRelease inside bindings/rn/android to ensure the bundled OpenSSL slices link correctly.
- [ ] Add CI job to run an iOS binding build (e.g., xcodebuild against the MaanyMpc pod) to verify the bundled xcframeworks.

